/**
 * Key name in `localStorage` to store all bookmarks
 *
 * @type {string}
 */
export const STORE_KEY_NAME = "bookmarks";

/**
 * Total number of bookmarks to display on page
 *
 * @type {number}
 */
export const TOTAL_BOOKMARKS_PER_PAGE = 20;

/**
 * Query param name which holds the value of current page
 *
 * @type {string}
 */
export const PAGE_QUERY_PARAM_NAME = "page";
